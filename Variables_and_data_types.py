var=1 #assigning a value to a variable creates/initiates it

var=2 #variable values can overwritten

var=2 #it's an integer
var=str(var) #it changed the variable type from integer to string type

var1,var2,var3=4,2,7  #assigning multiple variables at once


print(f'first variable is {var1} and the second one is {var2}')  #f-string printing






#Data types

var="hello"   #string (written inside quotation;will be covered later)

var=2    #integers (which are whole numbers)

var=3.5  #float (numbers with fractions)

var=[1,2,3,'hello','my']  #list (data structure that'll be covered later)

var=(0,0)   #tuples (data structure that'll be covered later)

var={'hello':1,'my':2}  #dictionary (data structure that'll be covered later)

var=range(5)   #raneg type


var={'yo',"bro"} #set type

var=frozenset({"yo","bro"})  #frozen set